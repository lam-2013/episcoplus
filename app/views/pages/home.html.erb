<% if signed_in? %>
    <div id="dashboard">
      <div class="row">
        <div class="columns large-8 push-4">
          <div class="panel">
            <%= render 'shared/post_form' %>
            <%= render 'shared/feed' %>
          </div>
        </div>

        <aside class="columns large-4 pull-8">
          <section>
            <%= render 'shared/user_info' %>
          </section>
        </aside>
      </div>
    </div>
<% end %>