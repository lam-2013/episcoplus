<% user = unfollow %>
<% # linked to the destroy action in the Relationship controller    %>
<%= form_for(current_user.relationships.find_by_followed_id(user), html: {method: :delete}, remote: true) do |f| %>
    <%= f.submit 'Smetti di seguire', class: 'small button grey' %>
<% end %>