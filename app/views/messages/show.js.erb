$mainMsg = $('#main-message');
$mainMsg.html("<%= escape_javascript(render(:partial => 'messages/full_message', :object => @message)) %>");
$thisMsg = $('#message-<%= @message.id %>').removeClass('unread');
$('.new-label', $thisMsg).remove();
