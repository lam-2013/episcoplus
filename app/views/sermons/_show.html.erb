<%= @sermon.title %>

<% if @sermon.subtitle %>
    <%= @sermon.subtitle %>
<% end %>

<% if @sermon.type %>
    <%= @sermon.type %>
<% end %>

<% if @sermon.day %>
    <%= @sermon.day %>
<% end %>

<%= @sermon.content %>

