<% if @feed_items.any? %>
  <ol class="posts">
    <% # we need to write in this way the partial rendering for passing the collection %>
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>